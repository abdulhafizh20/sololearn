impor java.util.Scanner;

Program kelas publik
{
 publik statis void main(String[] args) {
 Pemindai pemindai = Pemindai baru (System.in);
 Teks string = scanner.nextLine();
		char[] arr = text.toCharArray();

 kode anda pergi ke sini

		String balik kampung = "";
 untuk (char i : arr)
            balik = i + balik kampung;
        System.out.println(balik kampung);

	}
}
